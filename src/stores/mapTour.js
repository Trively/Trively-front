import { defineStore } from 'pinia'

export const useMapTourList = defineStore('mapTour', {
  // 상태
  state: () => ({
    tripList: [],
  }),
  // computed
  getters: {},
  // method
  actions: {},
})